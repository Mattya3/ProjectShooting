   // glPushMatrix();
        // glTranslatef(l.sx + l.xlen / 2, l.sy + l.ylen / 2, 0);
        // glRotatef(rotate, 0, 0, 1);
        // glTranslatef(-l.sx - l.xlen / 2, -l.sy - l.ylen / 2, 0);
        // myfighter.view_clone(l);
        // glPopMatrix();